#NOTE: this file is over ridden by the docker compose with the conf in the turn dir
listening-port=3478
listening-ip=0.0.0.0
external-ip=${EXTERNAL_IP}
# Bind to all interfaces inside container
lt-cred-mech

fingerprint
lt-cred-mech

realm=${ENDPOINT}
server-name=${ENDPOINT}

min-port=10000
max-port=10050

no-cli
